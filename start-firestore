#!/usr/bin/env bash

# Check user environment variable
if [[ -z "${FIRESTOREPROJECTID}" ]]; then
  echo "Missing FIRESTOREPROJECTID environment variable" >&2
  exit 1
fi

# Config gcloud project
gcloud config set project ${FIRESTOREPROJECTID}

# Start emulator
gcloud beta emulators firestore start --host-port=localhost:8081